# PhoneTool
遍历后n位号码

```
$ python3 phone_tool.py -h                          

        手机尾号遍历小工具V1.0

        -i      前数位固定数字，例：-i 1300610 或 --input=1300610
        -o      输出文件，例：-o wuhan.txt 或 --output=wuhan.txt
        -t      写入线程，默认10线程，例：-t 12 或 --thread=12
        
```
